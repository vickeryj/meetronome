<html>
<head>
  <meta http-equiv="Content-type" content="text/html; charset=utf-8" />
  <title><%= yield :title %></title>
  <%= stylesheet_link_tag("application") %>
  <%= javascript_include_tag(:defaults) %>
  <link rel="shortcut icon" href="/images//favicon.ico" type="image/vnd.microsoft.icon"/>
  <link rel="icon" href="/images/favicon.ico" type="image/vnd.microsoft.icon"/> 
  <%= yield :head %>
</head>
<body>
  <!-- there is a stupid IE bug that requires the image and the div all to be on one line, if there
  is a carriage return between then, IE6 will display a gap after the image -->
  <div id="header"><%= link_to(image_tag("header.png"),'/') %></div>
  <div id="content">
    <div class="clear" id="user_menu" >
      <%= meet_link_to("Login", {:controller => 'account', :action => 'login'}) unless logged_in? -%>
      <%= meet_link_to("Signup", {:controller => 'account', :action => 'signup'}) unless logged_in? -%>
      <%= meet_link_to("Logout", {:controller=> 'account', :action => 'logout'}) if logged_in? %>
    </div>
    <%= yield :layout %>
  </div> 
  <div>
    <div><%= image_tag("footer.png") %></div>
    <div id="footer">Meetronome is brought to you by Paul Damer and Josh Vickery</div>
  </div>
</body>
